{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "$id": "AdjustType.schema.json",
    "title": "AdjustType",
    "definitions": {
        "AdjustType": {
            "$id": "#AdjustType",
            "type": "string",
            "description": "Valid values for the Balance Adjust types",
            "enum": [
                "recurring",
                "oneTime"
            ]
        }
    }
}
